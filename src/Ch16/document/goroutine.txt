1. Go主线程与Go协程的关系
1) Go主线程上可以起多个Go协程，Go协程是轻量级的线程（编译器做优化）。
2) Go协程的特点为：
1- 有独立的栈空间；
2- 共享程序堆空间；
3- 调度由用户控制；
4- 系轻量级的线程。

2. Go主线程与Go协程的执行
1) 如果主线程退出了，而协程即使还没有执行完毕，也会退出。
2) 协程在主线程没有结束前退出，主线程依旧继续执行。

3. Goroutine的调度模型（MPG模式）
1) M：操作系统的主线程（物理线程）
2) P：协程执行需要的上下文。
3) G：协程。

